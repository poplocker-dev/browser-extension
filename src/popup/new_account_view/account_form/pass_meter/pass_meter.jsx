import React from 'react'
import './pass_meter.css'

const PassMeter = (props) => (
  <div className="password-meter">
    <div className="password-meter__status">Weak</div>
  </div>
)

export default PassMeter;
